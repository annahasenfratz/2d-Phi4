# Limitations

- This package is a reproduction scaffold, not a production sampler.
- The default frozen baseline is the current best controlled upsampling result,
  but local UV/action bias remains after A/R.
- Kernel retraining is intentionally excluded.
- The sample reproduction uses frozen checkpoints and canonical data only.

