# Cleanup Summary

The empirical initializer is isolated in a package module and frozen JSON
configuration. Production presets reference that configuration rather than any
July 21 validation run, paired field archive, notebook state, or empirical
local-Metropolis experiment. The old flow remains available only as the
existing coarse-coordinate transform required by the validated exact coarse
patch update; it is no longer used to create sweep-zero details.

No diagnostic runs, historical architectures, or legacy flow checkpoints were
deleted. The submit and extend command-line interfaces and output paths remain
unchanged.
